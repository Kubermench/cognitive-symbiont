#!/usr/bin/env bash
set -euo pipefail

# Safety: consider stashing before applying changes:
#   git rev-parse --is-inside-work-tree >/dev/null 2>&1 && git stash push -u -k -m pre-symbiont-$(date +%s) || true

# No explicit commands; fill in below
# echo 'do-something'

